comandos utilizados no back

python3 -m venv venv
source venv/bin/activate

pip install -r requirements.txt

python3 manage.py runserver


(venv)
Fazer as migrações do banco de dados:
python manage.py makemigrations
python manage.py migrate
